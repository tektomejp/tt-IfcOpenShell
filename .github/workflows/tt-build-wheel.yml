name: Build Python Wheel and Create Release

# Triggers on version tags only.
# Tag format: vX.Y.Z or vX.Y.Z.N (e.g., v0.8.5, v0.8.5.1)
on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]*"
  workflow_dispatch:
    inputs:
      version_override:
        description: "Version to embed in the wheel (e.g. 0.8.5). Leave empty to use latest tag."
        required: false

jobs:
  validate-tag:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      tag: ${{ steps.version.outputs.tag }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Determine version
        id: version
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            if [[ -n "${{ github.event.inputs.version_override }}" ]]; then
              VERSION="${{ github.event.inputs.version_override }}"
            else
              TAG=$(git describe --tags --match 'v[0-9]*.[0-9]*.[0-9]*' --abbrev=0 2>/dev/null || echo "v0.0.0")
              VERSION=${TAG#v}
            fi
            echo "Manual run, version: $VERSION"
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            echo "tag=v$VERSION" >> $GITHUB_OUTPUT
          else
            TAG=${GITHUB_REF#refs/tags/}
            VERSION=${TAG#v}
            echo "Tag push: $TAG -> version $VERSION"
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            echo "tag=$TAG" >> $GITHUB_OUTPUT
          fi

      - name: Validate tag format
        if: github.event_name == 'push'
        run: |
          TAG="${{ steps.version.outputs.tag }}"
          if ! [[ "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+\.?[0-9]*$ ]]; then
            echo "::error::Tag format invalid. Expected vX.Y.Z or vX.Y.Z.N, got: $TAG"
            exit 1
          fi
          echo "Tag format valid: $TAG"

      - name: Check version is monotonically increasing
        if: github.event_name == 'push'
        run: |
          CURRENT_TAG="${{ steps.version.outputs.tag }}"
          CURRENT_VERSION="${{ steps.version.outputs.version }}"

          # Only compare against tags that match our strict vX.Y.Z[.N] format.
          PREVIOUS_TAG=$(
            git tag -l 'v*' \
            | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+\.?[0-9]*$' \
            | grep -v "^${CURRENT_TAG}$" \
            | sort -V \
            | tail -n 1
          )

          if [ -z "$PREVIOUS_TAG" ]; then
            echo "No previous version tags found. First release!"
            exit 0
          fi

          PREVIOUS_VERSION=${PREVIOUS_TAG#v}
          echo "Current:  $CURRENT_VERSION"
          echo "Previous: $PREVIOUS_VERSION"

          HIGHER=$(printf '%s\n%s' "$CURRENT_VERSION" "$PREVIOUS_VERSION" | sort -V | tail -n 1)
          if [ "$HIGHER" != "$CURRENT_VERSION" ]; then
            echo "::error::Version $CURRENT_VERSION is not greater than existing $PREVIOUS_VERSION"
            exit 1
          fi

          echo "Version check passed: $CURRENT_VERSION > $PREVIOUS_VERSION"

  build-wheel:
    needs: validate-tag
    runs-on: ubuntu-22.04
    env:
      VERSION: ${{ needs.validate-tag.outputs.version }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Miniconda
        uses: conda-incubator/setup-miniconda@v3
        with:
          miniforge-version: latest
          activate-environment: build
          python-version: "3.12"

      - name: Install build dependencies
        shell: bash -el {0}
        run: |
          conda install -y -c conda-forge \
            cmake ninja swig patchelf \
            boost-cpp libboost-devel \
            occt cgal-cpp \
            eigen gmp mpfr \
            libxml2 nlohmann_json zlib \
            python=3.12 numpy setuptools

      - name: Configure CMake
        shell: bash -el {0}
        run: |
          mkdir build && cd build
          cmake ../cmake \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_IFCPYTHON=ON \
            -DBUILD_IFCGEOM=ON \
            -DBUILD_CONVERT=OFF \
            -DBUILD_GEOMSERVER=OFF \
            -DWITH_OPENCASCADE=ON \
            -DWITH_CGAL=ON \
            -DCOLLADA_SUPPORT=OFF \
            -DGLTF_SUPPORT=OFF \
            -DHDF5_SUPPORT=OFF \
            -DIFCXML_SUPPORT=ON \
            "-DSCHEMA_VERSIONS=2x3;4;4x3_add2"

      - name: Build C++ library
        shell: bash -el {0}
        run: |
          cd build
          ninja -j$(( $(nproc) / 2 + 1 ))

      - name: Assemble wheel
        shell: bash -el {0}
        run: |
          PYDIR=src/ifcopenshell-python/ifcopenshell
          DISTDIR=src/ifcopenshell-python/dist
          REPAIRED=src/ifcopenshell-python/repaired

          # Copy built .so and wrapper into the Python package tree
          cp build/ifcwrap/_ifcopenshell_wrapper*.so "$PYDIR/"
          cp build/ifcwrap/ifcopenshell_wrapper.py   "$PYDIR/"

          # Stamp version
          sed -i 's/^version = ".*"/version = "'"${VERSION}"'"/' src/ifcopenshell-python/pyproject.toml

          # Build a pure-Python wheel, then retag so auditwheel sees it as
          # a platform wheel (it refuses to repair "none-any" wheels).
          pip install build auditwheel wheel
          python -m build --wheel src/ifcopenshell-python --outdir "$DISTDIR"
          wheel tags --remove \
            --python-tag=cp312 --abi-tag=cp312 --platform-tag=linux_x86_64 \
            "$DISTDIR"/*.whl

          # Bundle all shared libs (OCCT, GMP, MPFR, libxml2, etc.) into the
          # wheel.  auditwheel auto-detects the manylinux tag from glibc symbols.
          LD_LIBRARY_PATH="$CONDA_PREFIX/lib" \
            auditwheel repair "$DISTDIR"/*.whl \
              -w "$REPAIRED"

          # Retag to linux_x86_64 so pip accepts the wheel regardless of the
          # host glibc version.  The bundled libs and patched RPATHs from
          # auditwheel are preserved.
          wheel tags --remove \
            --platform-tag=linux_x86_64 \
            "$REPAIRED"/*.whl

          echo "Final wheel:"
          ls -lh "$REPAIRED"/*.whl

      - name: Verify wheel installs and works
        shell: bash -el {0}
        run: |
          pip install src/ifcopenshell-python/repaired/*.whl
          python -c "
          import ifcopenshell
          print(f'version : {ifcopenshell.version}')
          print(f'schemas : {ifcopenshell.ifcopenshell_wrapper.schema_names()}')
          f = ifcopenshell.file(schema='IFC4')
          f.createIfcProject('smoke-test')
          print('Smoke test passed')
          "

      - name: Upload wheel as artifact
        uses: actions/upload-artifact@v4
        with:
          name: ifcopenshell-wheel
          path: src/ifcopenshell-python/repaired/*.whl
          retention-days: 90

      - name: Create GitHub Release
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: src/ifcopenshell-python/repaired/*.whl
          generate_release_notes: true
          draft: false
          prerelease: false
          body: |
            ## ifcopenshell ${{ env.VERSION }} (tt-IfcOpenShell fork)

            Self-contained wheel with bundled OCCT, CGAL dependencies.
            Built from commit ${{ github.sha }} with lazy loading support.

            ### Install
            ```
            pip install https://github.com/tektomejp/tt-IfcOpenShell/releases/download/${{ needs.validate-tag.outputs.tag }}/ifcopenshell-${{ env.VERSION }}-cp312-cp312-linux_x86_64.whl
            ```

            ### Platform
            - Linux x86_64, Python 3.12
            - Self-contained: OCCT, GMP, MPFR, libxml2 bundled inside
          token: ${{ secrets.GITHUB_TOKEN }}
